name: daily strategy (archived)

# Archived on 2026-05-27. Autonomous strategy/report generation is no longer
# running. Scheduled triggers are intentionally removed.

on:
  workflow_dispatch: {}

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: archived
        run: echo "Daily strategy generation is archived."
