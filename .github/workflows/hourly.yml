name: hourly cron (archived)

# Archived on 2026-05-27. Autonomous X posting is no longer running.
# Scheduled triggers are intentionally removed.

on:
  workflow_dispatch: {}

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: archived
        run: echo "Hourly autonomous posting is archived."
